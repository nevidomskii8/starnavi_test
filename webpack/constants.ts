// Название папок для построения пути в webpack
export const FOLDERS = {
  SRC: "src",
  DIST: "build",
};

// Название файлов для построения пути в webpack
export const FILES = {
  ENTRYJS: "index.tsx",
  ENTRYHTML: "index.html",
}
